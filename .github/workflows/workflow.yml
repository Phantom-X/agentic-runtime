name: Publish to PyPI (Placeholder)

on:
  push:
    branches: [ main ]

jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Print message (placeholder)
        run: |
          echo "This is a placeholder workflow for agentic-runtime."
          echo "No actual package build or upload will happen."
